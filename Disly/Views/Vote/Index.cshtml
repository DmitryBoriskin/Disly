@model VoteViewModel
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Голосование</h1>
            @Html.Partial("services/breadcrumbs")
            @if (Model.Item == null)
            {
                @Html.Partial("List")
            }
            else
            {
                @Html.Partial("Item")
            }
        </div>
   </div>
</div>