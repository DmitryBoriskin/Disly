<h1>Новости</h1>

    @if (!String.IsNullOrEmpty(ViewBag.Alias))
    {
        <div>запись @ViewBag.Alias.ToString()</div>
    }
    else
    {
        <div>Список</div>
        <div>
            @if (!String.IsNullOrEmpty(ViewBag.Category))
            {
                <span>Категория</span>
                @ViewBag.Category.ToString()
            }
        </div>
    }
