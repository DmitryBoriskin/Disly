@if (Model.PageMenuChild != null)
{
    <div class="child">
        <ul>
            @foreach (var item in Model.PageMenuChild)
            {
                <li><a href="@item.Path@item.Alias">@item.Title</a></li>
            }
        </ul>
    </div>
}

